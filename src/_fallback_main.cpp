#include <iostream>

int main(int argc, char** argv) {
    std::cout << "R3C CLI (fallback mode)" << std::endl;
    std::cout << "No main.cpp found — running fallback stub." << std::endl;
    return 0;
}
