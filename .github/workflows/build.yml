name: 🔖 R3C v8.0 Pure GCC Autonomous Release (3OS + LTSS + Integrity)

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '.github/workflows/**'
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pure-gcc-stable
  cancel-in-progress: false

jobs:
  build:
    name: "Pure GCC 3OS Build"
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - name: 🧭 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ======================================================
      # 🧱 GCC Setup for Each OS
      # ======================================================
      - name: ⚙️ Setup Pure GCC Environment
        shell: bash
        run: |
          echo "🔧 Setting up GCC on ${{ matrix.os }}..."

          if [[ "$RUNNER_OS" == "Windows" ]]; then
            choco install mingw -y
            export PATH="/c/tools/mingw64/bin:/c/ProgramData/chocolatey/bin:$PATH"
            which gcc || { echo "❌ GCC not found!"; exit 1; }
            rm -rf "/c/Program Files/Microsoft Visual Studio" || true
            rm -rf "/c/Program Files (x86)/Microsoft Visual Studio" || true
            echo "✅ MSVC removed. Pure GCC only."

          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            brew install gcc || true
            export CC=gcc-13
            export CXX=g++-13
            echo "✅ Using $(gcc-13 --version | head -n1)"

          elif [[ "$RUNNER_OS" == "Linux" ]]; then
            sudo apt-get update -y
            sudo apt-get install -y build-essential
            echo "✅ Linux GCC OK"
          fi

      # ======================================================
      # 🧩 Build
      # ======================================================
      - name: 🧱 Build R3C
        shell: bash
        run: |
          mkdir -p build
          cmake -E env CC=gcc CXX=g++ cmake -B build -DCMAKE_BUILD_TYPE=Release -G "Unix Makefiles"
          cmake --build build --target r3c_cli
          ls -lah build

      # ======================================================
      # 🔎 Test & Archive
      # ======================================================
      - name: 🔎 Smoke Test
        shell: bash
        run: |
          echo "⚙️ Smoke test..."
          exe=$(find build -type f -name "r3c_cli*" | head -n1)
          if [ -n "$exe" ]; then
            "$exe" --version || echo "⚠️ CLI version check skipped."
          else
            echo "❌ r3c_cli not found!"
            exit 1
          fi

      - name: 📦 Package
        shell: bash
        run: |
          mkdir -p dist
          tar -czf "dist/r3c-${{ matrix.os }}.tar.gz" -C build .
          echo "✅ Packaged build for ${{ matrix.os }}"

      - name: 💾 Upload
        uses: actions/upload-artifact@v4
        with:
          name: r3c-${{ matrix.os }}
          path: dist/
          retention-days: 7
