name: 🔖 Auto Stable Tag (immutable)

on:
  workflow_run:
    workflows: ["🧩 R3C Cross-Platform Build (stable)"]
    types:
      - completed

permissions:
  contents: write  # 🔥 꼭 필요: 태그 푸시 권한

jobs:
  tag:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create immutable timestamp tag
        run: |
          TS=$(date -u +'%Y%m%d-%H%M%S')
          TAG="auto-stable-${TS}"
          echo "Creating tag $TAG"
          git config user.name "r3c-bot"
          git config user.email "r3c-bot@users.noreply.github.com"
          git tag "$TAG"
          git push origin "$TAG"
