name: 🔖 Auto Stable Tag & Release (GH_TOKEN Forced)

on:
  workflow_dispatch:  # ⚙️ 수동 실행 테스트 후 자동 워크플로에 연결 가능
  # workflow_run:
  #   workflows: ["🧩 R3C Cross-Platform Build (stable)"]
  #   types: [completed]

permissions:
  contents: none  # 기본 GITHUB_TOKEN 차단

env:
  GH_TOKEN: ${{ secrets.GH_TOKEN }}
  GH_REPO: r3c-foundation/r3c
  GH_USER: r3c-bot
  GH_EMAIL: r3c-bot@users.noreply.github.com

jobs:
  tag_release:
    runs-on: ubuntu-latest

    steps:
      # -------------------------------------------------------
      # 🧰 Checkout (using PAT)
      # -------------------------------------------------------
      - name: 🧰 Checkout with PAT
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      # -------------------------------------------------------
      # ⚙️ Configure Git (Force PAT)
      # -------------------------------------------------------
      - name: ⚙️ Configure Git
        run: |
          git config --global user.name "${GH_USER}"
          git config --global user.email "${GH_EMAIL}"
          git remote set-url origin https://x-access-token:${GH_TOKEN}@github.com/${GH_REPO}.git
          git remote -v

      # -------------------------------------------------------
      # 🏷️ Create and push tag (PAT 인증)
      # -------------------------------------------------------
      - name: 🏷️ Create and push tag
        id: tag
        run: |
          TS=$(date -u +'%Y%m%d-%H%M%S')
          TAG="auto-stable-${TS}"
          echo "TAG=${TAG}" >> $GITHUB_ENV
          echo "✅ Creating tag: ${TAG}"
          git tag "${TAG}"
          git push origin "${TAG}"

      # -------------------------------------------------------
      # 📦 Create GitHub Release (PAT 인증)
      # -------------------------------------------------------
      - name: 📦 Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GH_TOKEN }}
          tag_name: ${{ env.TAG }}
          name: "R3C Stable — ${{ env.TAG }}"
          body: |
            🧩 **R3C Immutable Stable Release**
            - Tag: `${{ env.TAG }}`
            - Created automatically using Personal Access Token (GH_TOKEN)
            - Repository: https://github.com/${{ env.GH_REPO }}
          draft: false
          prerelease: false
